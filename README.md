
  # File Management

  This is a code bundle for File Management. The original project is available at https://www.figma.com/design/uUMF2UR6wW30xlcezpvqrS/File-Management.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.

  ## Deploying to Azure Container Apps

  End-to-end automated deployment (build, push, and deploy) is documented in:

  - `DEPLOYMENT_CONTAINER_APPS.md`

  The CI workflow is:

  - `.github/workflows/deploy-container-apps.yml`
  